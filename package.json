{
  "name": "multi-select-webcomponent",
  "version": "1.0.0",
  "description": "A fully styleable multiselect with no polyfills",
  "main": "dist/multi-select-webcomponent.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Honatas (Jonatas de Moraes Junior)",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.2.1",
    "rollup": "^2.46.0"
  }
}
